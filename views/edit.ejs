<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post | Admin View</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="min-h-screen flex flex-col items-center bg-blue-100">
  <header class="w-full bg-white shadow-sm py-4 mb-8">
    <div class="max-w-5xl mx-auto flex justify-between items-center px-6">
      <h1 class="text-2xl font-bold text-gray-800">Admin View</h1>
      <nav class="space-x-4">
        <a href="/read" class="text-blue-600 hover:underline">View Posts</a>
      </nav>
    </div>
  </header>

  <main class="flex-grow w-full max-w-3xl bg-white shadow-lg rounded-xl p-8">
    <h2 class="text-2xl font-semibold text-gray-800 mb-10">Edit Post</h2>

    <form action="/edit/<%= post._id %>" method="POST" class="space-y-5">

       <div>
          <select
            id="category"
            name="category"
            class="w-full border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-blue-400 focus:border-blue-400 outline-none font-semibold"
          >
            <option value="academics">Academics</option>
            <option value="sideQuests">Side Quests</option>
            <option value="tech">Tech</option>
          </select>
      </div>


      <div>
        <label class="block text-gray-700 font-medium mb-1">Title</label>
        <input type="text" name="title" placeholder="Enter title" 
               class="w-full border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-blue-400 focus:border-blue-400 outline-none" value=<%= post.title %> required>
      </div>

      <div>
        <label class="block text-gray-700 font-medium mb-1">Subtitle</label>
        <input type="text" name="subtitle" placeholder="Enter subtitle" 
               class="w-full border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-blue-400 focus:border-blue-400 outline-none" value=<%= post.subtitle %> >
      </div>

      <!-- Content -->
      <div>
        <label class="block text-gray-700 font-medium mb-1">Content</label>
        <textarea name="content" placeholder="Write your content here..." rows="8"
                  class="w-full border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-blue-400 focus:border-blue-400 outline-none" required><%= post.content %></textarea>
      </div>

      <!-- Date -->
      <div>
        <label class="block text-gray-700 font-medium mb-1">Date</label>
        <input type="date" name="date"
               class="w-full border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-blue-400 focus:border-blue-400 outline-none" value=<%= post.date %> >
      </div>

      <!-- Submit -->
      <div class="text-center">
        <button type="submit"
                class="bg-blue-600 text-white px-6 py-2 rounded-lg font-medium hover:bg-blue-700 transition duration-200">
          Save Changes
        </button>
      </div>
    </form>
  </main>

  <!-- Footer -->
  <footer class="w-full py-4 mt-8 text-center text-sm text-gray-500">
    © Lousy Writers Admin
  </footer>

</body>
</html>
